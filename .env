# ────────────────────────────────────────────────────────
# Costerra ERP — Environment Variables
# DATABASE_URL is set dynamically by main/database/prisma-client.ts
# using Electron's app.getPath('userData').
# This file is only used for Prisma CLI operations (migrations).
# ────────────────────────────────────────────────────────

DATABASE_URL="file:./dev.db"
